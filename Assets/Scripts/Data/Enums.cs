public enum EquipmentType
{
    Sword,
    Shield
}

public enum StatType
{
    Attack,
    Hp
}

public enum MonsterType
{
    Normal,
    Boss,
    Elite
}

public enum StatGrade
{
    Normal,
    Rare,
    Epic,
    Unique,
    Legendary
}
